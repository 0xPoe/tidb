# Statistics Documentation Audit - release-8.5 Other Findings

Audit date: 2026-06-22

Scope: TiDB `release-8.5` in `/Users/poe/code/tidb` compared with docs `release-8.5` in `/Users/poe/code/docs`. This file excludes system-variable default-value mismatches; those are in [stats-doc-audit-release-8.5.md](/Users/poe/code/tidb/stats-doc-audit-release-8.5.md:1).

## Missing Or Outdated Docs

| Area | Item | Code evidence | Docs evidence | Finding | Suggested docs update |
|---|---|---|---|---|---|
| TiDB config | `enable-load-fmsketch` | `release-8.5:pkg/config/config.go:746`; actively read in `release-8.5:pkg/statistics/handle/bootstrap.go:806`, `storage/stats_read_writer.go:360`, and `syncload/stats_syncload.go:419`. | No match in `tidb-configuration-file.md` or command-line config docs. | Active stats-loading config item is undocumented on 8.5. | Add docs for `performance.enable-load-fmsketch`, including default `false`, effect, and when users should enable it. |
| System variable | `tidb_enable_fast_analyze` | `release-8.5:pkg/sessionctx/variable/sysvar.go:2383` warns "the fast analyze feature has already been removed in TiDB v7.5.0, so this will have no effect". | `system-variables.md:2201` says the variable enables Fast Analyze; `extended-statistics.md:27` treats it as an active mode; `basic-features.md` still lists the feature. | Docs say deprecated but still describe active behavior. | Update current 8.5 docs to state that setting it has no effect because Fast Analyze was removed in v7.5.0. |
| System variable | `tidb_enable_auto_analyze_priority_queue` | `release-8.5:pkg/sessionctx/variable/sysvar.go:1139` emits a deprecation warning and says TiDB will always use the priority queue in the future. | `system-variables.md:2021` and `statistics.md:40` say the variable controls whether the priority queue is enabled. | Docs omit the deprecation state. | Add a deprecation warning. For 8.5, say changing it is discouraged and future TiDB versions always use the priority queue. |
| System variable | `tidb_auto_analyze_partition_batch_size` | `release-8.5:pkg/sessionctx/variable/sysvar.go:1041` emits deprecated-with-no-replacement warning. | `system-variables.md:1235` describes it as an active tuning variable with no deprecation warning. | Docs miss the deprecation state. | Add a deprecation warning and avoid recommending it for tuning. |
| System variable | `tidb_enable_column_tracking` | `release-8.5:pkg/sessionctx/variable/sysvar.go:1263` always returns `ON`; `:1271` warns it is deprecated and always set to `ON`; `SetGlobal` is a no-op. | `system-variables.md:2125` says it controls predicate-column collection and that disabling it clears previous data. | Docs describe behavior that no longer happens. | Update docs to state it is deprecated, always `ON`, and setting it is a no-op. Point users to `tidb_analyze_column_options`. |
| System variable | `tidb_stats_update_during_ddl` | `release-8.5:pkg/sessionctx/variable/sysvar.go:2818` registers it as SESSION \| GLOBAL; `:2820` rejects enabling it when `tidb_analyze_version=1`. | `system-variables.md:1670` says Scope: GLOBAL and does not mention the `tidb_analyze_version=2` requirement. | Scope and prerequisite are documented incompletely. | Change scope to SESSION \| GLOBAL; add a note that enabling requires `tidb_analyze_version=2`. |
| TiDB config | `lite-init-stats` | `release-8.5:pkg/config/config.go:748` and `release-8.5:pkg/statistics/handle/bootstrap.go:762` say lite mode loads stats metadata and histograms, but not TopN/buckets/FMSketch; `bootstrap.go:790` logs completion of histogram loading in lite mode. | `tidb-configuration-file.md:630` says lite mode does not load any histogram, TopN, or Count-Min Sketch of indexes and columns. | Docs conflict with code comments and implementation. | Correct the lite-mode description: it loads metadata and histogram/existence information, while heavier TopN/bucket/FMSketch data is deferred. |
| SQL statement | `FLUSH STATS_DELTA ... [CLUSTER]` | Parser/executor support and tests exist: `release-8.5:pkg/parser/ast/misc.go:883`, `release-8.5:pkg/executor/simple.go:2798`, `release-8.5:pkg/executor/test/simpletest/simple_test.go:876`; supports table, database wildcard, global wildcard, and `CLUSTER`. | Docs only list `STATS_DELTA` as a keyword; no SQL statement page or overview entry. | User-visible stats statement is undocumented. | Add a statement page or document as internal-only if that is the intended support level. Include privileges and `CLUSTER` behavior. |
| SQL statement | `DROP STATS ... PARTITION ...` and `DROP STATS ... GLOBAL` | Parser emits deprecation warnings in `release-8.5:pkg/parser/parser.y:5428` and `:5437`; tests assert warnings in `release-8.5:pkg/executor/test/simpletest/simple_test.go:624` and `:635`. | `sql-statements/sql-statement-drop-stats.md` documents both forms as normal usage. | Deprecated syntax is not marked deprecated. | Add warnings for `PARTITION` and `GLOBAL`; recommend plain `DROP STATS table_name` where applicable. |
| Statistics feedback docs | Query feedback and feedback-driven stats updates | Active code search found only compatibility table creation for `mysql.stats_feedback`, not feedback-driven stats update logic. Removed config options include `performance.feedback-probability` and `performance.query-feedback-limit` in `release-8.5:pkg/config/config.go:1164`. | `tidb-configuration-file.md:562` describes feedback update schedules; `grafana-tidb-dashboard.md:171`-`:174` documents feedback panels that are not present in the current dashboard JSON. | Current docs describe removed/deprecated feedback behavior as active. | Remove feedback-update behavior from `stats-lease` docs and clean up stale Grafana dashboard descriptions. Keep `mysql.stats_feedback` only as a compatibility/system-table note if needed. |
| System variable docs | `tidb_mem_quota_analyze` note about `run-auto-analyze` | `performance.run-auto-analyze` is listed as unsupported/removed in `release-8.5:pkg/config/config.go:1148` and config tests. | `system-variables.md:3991` says `auto_analyze` is triggered only when `run-auto-analyze` is enabled in the startup config. | Docs refer to a removed config item. | Replace with `tidb_enable_auto_analyze` and, if relevant, `tidb_enable_stats_owner`. |

## Checked Non-Default Items

| Area | Items checked | Result |
|---|---|---|
| Extended statistics | `tidb_enable_extended_stats`, `ADD/DROP STATS_EXTENDED`, `SHOW STATS_EXTENDED`, `ADMIN RELOAD STATS_EXTENDED` | Code still implements the feature behind `tidb_enable_extended_stats`; current 8.5 docs exist. No branch-specific gap found. |
| Stats SQL statements | `ANALYZE TABLE`, `LOAD STATS`, `LOCK STATS`, `UNLOCK STATS`, `SHOW STATS_*`, `SHOW ANALYZE STATUS`, `SHOW COLUMN_STATS_USAGE` | Docs pages exist. `DROP STATS` and `FLUSH STATS_DELTA` gaps are listed above. |
| TiDB config | `stats-lease`, `pseudo-estimate-ratio`, `stats-load-concurrency`, `stats-load-queue-size`, `enable-stats-cache-mem-quota`, `force-init-stats`, `concurrently-init-stats`, `tidb_enable_stats_owner` | Docs pages exist. Stale wording for feedback and `lite-init-stats` is listed above. |
