# Statistics Documentation Audit - master Other Findings

Audit date: 2026-06-22

Scope: TiDB `master` in `/Users/poe/code/tidb` compared with docs `master` in `/Users/poe/code/docs`. This file excludes system-variable default-value mismatches; those are in [stats-doc-audit-master.md](/Users/poe/code/tidb/stats-doc-audit-master.md:1).

## Missing Or Outdated Docs

| Area | Item | Code evidence | Docs evidence | Finding | Suggested docs update |
|---|---|---|---|---|---|
| Feature and system variable | Extended statistics, `tidb_enable_extended_stats`, `SHOW STATS_EXTENDED`, `ADD/DROP STATS_EXTENDED`, `ADMIN RELOAD STATS_EXTENDED` | `master:pkg/ddl/executor.go:4001` returns "Extended statistics feature has been removed"; `master:pkg/executor/show.go:234` and `master:pkg/executor/simple.go:3307` return the same removal error; `tidb_enable_extended_stats` is compatibility-only and hidden in `sysvar.go:2736`. | `extended-statistics.md` still teaches the feature; `system-variables.md:2220` says TiDB can collect extended statistics; TOC and `basic-features.md` still link it. | Current docs describe a removed feature as usable. | Remove from active TOC/current feature docs or replace with a compatibility/removal note. Update `tidb_enable_extended_stats` to say it is hidden, compatibility-only, and has no usable feature behind it. |
| System variable | `tidb_enable_fast_analyze` | `master:pkg/sessionctx/variable/sysvar.go:2598` warns "the fast analyze feature has already been removed in TiDB v7.5.0, so this will have no effect". | `system-variables.md:2258` says the variable enables Fast Analyze; `extended-statistics.md:27` treats it as an active mode; `basic-features.md` still lists the feature. | Docs say deprecated but still describe active behavior. | Update current docs to state that setting it has no effect because Fast Analyze was removed in v7.5.0. |
| System variable and feature | `tidb_analyze_version` / Statistics Version 1 | `master:pkg/sessionctx/variable/sysvar.go:2823` rejects `tidb_analyze_version=1` with "no longer supported". | `system-variables.md:1198` still lists range `[1, 2]`; `statistics.md:359` says both versions are supported; `statistics.md:303` says setting version 1 can disable analyze option persistence. | Master docs still document Statistics Version 1 as settable/supported. | Update `system-variables.md` and `statistics.md` to say only version 2 can be set on master/current dev docs; clarify any legacy-read behavior separately if needed. |
| System variable | `tidb_enable_auto_analyze_priority_queue` | `master:pkg/sessionctx/variable/sysvar.go:1224` rejects `OFF` because the variable is deprecated and TiDB always uses the priority queue. | `system-variables.md:2069` and `statistics.md:41` say the variable controls whether the priority queue is enabled. | Docs describe a disable switch that master no longer allows. | Mark it deprecated and effectively always enabled; document that setting `OFF` is rejected. |
| System variable | `tidb_auto_analyze_partition_batch_size` | `master:pkg/sessionctx/variable/sysvar.go:1122` emits deprecated-with-no-replacement warning. | `system-variables.md:1283` describes it as an active tuning variable with no deprecation warning. | Docs miss the deprecation state. | Add a deprecation warning and avoid recommending it for tuning. |
| System variable | `tidb_enable_column_tracking` | `master:pkg/sessionctx/variable/sysvar.go:1444` always returns `ON`; `:1452` warns that it is deprecated and always set to `ON`; `SetGlobal` is a no-op. | `system-variables.md:2182` says it controls predicate-column collection and that disabling it clears previous data. | Docs describe behavior that no longer happens. | Update docs to state it is deprecated, always `ON`, and setting it is a no-op. Point users to `tidb_analyze_column_options`. |
| System variable | `tidb_stats_update_during_ddl` | `master:pkg/sessionctx/variable/sysvar.go:3059` registers it as SESSION \| GLOBAL. | `system-variables.md:1718` says Scope: GLOBAL; no `system-variable-reference.md` entry on master. | Scope is documented incorrectly and the reference index is missing. | Change scope to SESSION \| GLOBAL and add the missing reference entry. |
| TiDB config | `[instance].tidb_stats_cache_mem_quota` | `master:pkg/config/config.go:690` adds the `[instance]` config item. | No match in `tidb-configuration-file.md` or command-line config docs. | Config file docs miss an instance-scope stats-cache quota item. | Add it to the `[instance]` section or explicitly document that users should configure only the system variable. |
| TiDB config | `concurrently-init-stats` | `master:pkg/config/config.go:924` says the setting has no effect because stats are always initialized concurrently. | `tidb-configuration-file.md:621` has a deprecation note, but `:623` still says it controls startup behavior and takes effect when `lite-init-stats=false`. | Docs contain contradictory/outdated behavior. | Keep the deprecation note and remove the active-control wording. |
| TiDB config | `lite-init-stats` | `master:pkg/config/config.go:912` and `master:pkg/statistics/handle/bootstrap.go:835` say lite mode loads stats metadata and histograms, but not TopN/buckets/FMSketch; `bootstrap.go:874` logs completion of histogram loading in lite mode. | `tidb-configuration-file.md:630` says lite mode does not load any histogram, TopN, or Count-Min Sketch of indexes and columns. | Docs conflict with code comments and implementation. | Correct the lite-mode description: it loads metadata and histogram/existence information, while heavier TopN/bucket/FMSketch data is deferred. |
| SQL statement | `FLUSH STATS_DELTA ... [CLUSTER]` | Parser/executor support and tests exist: `master:pkg/parser/ast/misc.go:1062`, `master:pkg/executor/simple.go:2978`, `master:pkg/executor/simple_test.go:411`; supports table, database wildcard, global wildcard, and `CLUSTER`. | Docs only list `STATS_DELTA` as a keyword; no SQL statement page or overview entry. | User-visible stats statement is undocumented. | Add a statement page or document as internal-only if that is the intended support level. Include privileges and `CLUSTER` behavior. |
| SQL statement | `DROP STATS ... PARTITION ...` and `DROP STATS ... GLOBAL` | Parser emits deprecation warnings in `master:pkg/parser/parser.y:5774` and `:5783`; tests assert warnings in `master:pkg/executor/test/simpletest/simple_test.go:639` and `:650`. | `sql-statements/sql-statement-drop-stats.md` documents both forms as normal usage. | Deprecated syntax is not marked deprecated. | Add warnings for `PARTITION` and `GLOBAL`; recommend plain `DROP STATS table_name` where applicable. |
| Statistics feedback docs | Query feedback and feedback-driven stats updates | Active code search found only compatibility table creation for `mysql.stats_feedback`, not feedback-driven stats update logic. Removed config options include `performance.feedback-probability` and `performance.query-feedback-limit` in `master:pkg/config/config.go:1388`. | `tidb-configuration-file.md:558` describes feedback update schedules; `grafana-tidb-dashboard.md:172`-`:175` documents feedback panels that are not present in the current dashboard JSON. | Current docs describe removed/deprecated feedback behavior as active. | Remove feedback-update behavior from `stats-lease` docs and clean up stale Grafana dashboard descriptions. Keep `mysql.stats_feedback` only as a compatibility/system-table note if needed. |
| System variable docs | `tidb_mem_quota_analyze` note about `run-auto-analyze` | `performance.run-auto-analyze` is listed as unsupported/removed in `master:pkg/config/config.go:1372` and config tests. | `system-variables.md:4116` says `auto_analyze` is triggered only when `run-auto-analyze` is enabled in the startup config. | Docs refer to a removed config item. | Replace with `tidb_enable_auto_analyze` and, if relevant, `tidb_enable_stats_owner`. |

## Checked Non-Default Items

| Area | Items checked | Result |
|---|---|---|
| Stats SQL statements | `ANALYZE TABLE`, `LOAD STATS`, `LOCK STATS`, `UNLOCK STATS`, `SHOW STATS_*`, `SHOW ANALYZE STATUS`, `SHOW COLUMN_STATS_USAGE`, `REFRESH STATS` | Docs pages exist. `DROP STATS` and `FLUSH STATS_DELTA` gaps are listed above. |
| TiDB config | `stats-lease`, `pseudo-estimate-ratio`, `stats-load-concurrency`, `stats-load-queue-size`, `enable-stats-cache-mem-quota`, `skip-init-stats`, `force-init-stats`, `tidb_enable_stats_owner` | Docs pages exist. Stale wording for feedback, `lite-init-stats`, and `concurrently-init-stats` is listed above. |
